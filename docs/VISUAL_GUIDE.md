# 🎨 Solana Security Patterns - Visual Guide

## Quick Reference: The 7 Critical Vulnerabilities

```
┌─────────────────────────────────────────────────────────────────┐
│                    SOLANA SECURITY VULNERABILITIES               │
│                     (Ranked by Severity)                        │
└─────────────────────────────────────────────────────────────────┘

🔴 CRITICAL VULNERABILITIES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. MISSING SIGNER CHECK
   ├─ Impact: Complete fund loss
   ├─ Real Loss: $325M (Wormhole)
   ├─ Fix: Use Signer<'info> type
   └─ Test: Pass wrong signer

2. MISSING OWNER CHECK
   ├─ Impact: Account substitution
   ├─ Real Loss: Multiple protocols
   ├─ Fix: Use Account<'info, T>
   └─ Test: Pass fake account

3. ACCOUNT DATA MATCHING (PDA)
   ├─ Impact: State manipulation
   ├─ Real Loss: $52M (Cashio)
   ├─ Fix: Use seeds + bump constraints
   └─ Test: Pass wrong PDA

4. ARBITRARY CPI
   ├─ Impact: Malicious code execution
   ├─ Real Loss: $8.8M (Crema)
   ├─ Fix: Use Program<'info, T>
   └─ Test: Pass malicious program

🟡 HIGH VULNERABILITIES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

5. ARITHMETIC OVERFLOW/UNDERFLOW
   ├─ Impact: Balance manipulation
   ├─ Real Loss: $50M+ (Various)
   ├─ Fix: Use checked_* operations
   └─ Test: Use MAX/MIN values

6. RE-ENTRANCY VIA CPI
   ├─ Impact: Double-spending
   ├─ Pattern: DAO-style attacks
   ├─ Fix: Checks-Effects-Interactions
   └─ Test: Recursive calls

7. TYPE COSPLAY
   ├─ Impact: Logic bypass
   ├─ Real Loss: Solend issues
   ├─ Fix: Let Anchor check discriminators
   └─ Test: Pass wrong account type


TOTAL REAL-WORLD LOSSES: $435M+
```

---

## Vulnerability Flow Diagrams

### 1. Missing Signer Check Attack Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                  MISSING SIGNER CHECK EXPLOIT                   │
└─────────────────────────────────────────────────────────────────┘

VULNERABLE CODE:
┌──────────────────────────────────────────────────────────────┐
│ pub fn withdraw(ctx: Context<Withdraw>, amount: u64)         │
│   let vault = &ctx.accounts.vault;                           │
│   require!(vault.authority == authority.key());  ❌ NO SIG!  │
│   transfer_lamports(vault, authority, amount);               │
└──────────────────────────────────────────────────────────────┘

ATTACK SEQUENCE:
┌─────────┐     ┌─────────┐     ┌─────────┐     ┌─────────┐
│ Step 1  │────▶│ Step 2  │────▶│ Step 3  │────▶│ Step 4  │
│ Read    │     │ Create  │     │ Sign    │     │ Exploit │
│ Vault   │     │ TX with │     │ With    │     │ Success │
│ Data    │     │ Victim  │     │ Own Key │     │ 💰💰💰  │
└─────────┘     └─────────┘     └─────────┘     └─────────┘
    │               │               │               │
    ▼               ▼               ▼               ▼
Find vault     Include victim   Attacker       Funds
authority =    authority as     signs, not     stolen
Alice.key()    account          Alice!         ✓

SECURE CODE:
┌──────────────────────────────────────────────────────────────┐
│ #[derive(Accounts)]                                           │
│ pub struct Withdraw<'info> {                                  │
│   pub authority: Signer<'info>,  ✅ ENFORCES SIGNATURE       │
│ }                                                             │
└──────────────────────────────────────────────────────────────┘
```

---

### 2. Account Data Matching (Cashio-Style) Attack

```
┌─────────────────────────────────────────────────────────────────┐
│              ACCOUNT DATA MATCHING EXPLOIT                      │
│                  (Cashio Pattern)                               │
└─────────────────────────────────────────────────────────────────┘

VULNERABLE: No PDA Verification
┌────────────────────────────────────────────────────────────┐
│  Expected PDA: seeds = [b"collateral", mint.key()]         │
│  Program checks: ❌ NOTHING                                │
│  Attacker does: Create fake account with any seeds         │
└────────────────────────────────────────────────────────────┘

ATTACK:
  Real Collateral PDA              Fake Collateral Account
┌──────────────────┐            ┌──────────────────────┐
│ Mint: USDC       │            │ Mint: USDC           │
│ Amount: 1,000    │            │ Amount: 1,000,000 ❌ │
│ Seeds: ✅ Valid  │            │ Seeds: ❌ Wrong      │
│ Bump: 254        │            │ Bump: 253            │
└──────────────────┘            └──────────────────────┘
         ↓                                ↓
    Not Used                     Attacker Passes This!
                                           ↓
                              Mints 1M stablecoins
                              With only 1k collateral
                              $52M stolen ✓

SECURE CODE:
┌────────────────────────────────────────────────────────────┐
│ #[account(                                                  │
│   seeds = [b"collateral", mint.key().as_ref()],  ✅        │
│   bump = collateral.bump,                        ✅        │
│   has_one = mint                                 ✅        │
│ )]                                                          │
│ pub collateral: Account<'info, Collateral>,                │
└────────────────────────────────────────────────────────────┘
```

---

### 3. Arithmetic Overflow Visualization

```
┌─────────────────────────────────────────────────────────────────┐
│                    ARITHMETIC OVERFLOW                          │
└─────────────────────────────────────────────────────────────────┘

UNCHECKED ADDITION:
┌──────────────────────────────────────────────────────────────┐
│                      u64 Number Line                          │
├──────────────────────────────────────────────────────────────┤
│ 0 ────────────────────────────────────── 18,446,744,073,709,│
│                                            551,615 (MAX)     │
│                                                 ↓             │
│                                            vault.balance      │
│                                            = MAX              │
│                                                 ↓             │
│                                            + 1  ❌            │
│                                                 ↓             │
│                                            WRAPS TO 0!        │
│ 0 ◀─────────────────────────────────────────────┘            │
└──────────────────────────────────────────────────────────────┘

REAL-WORLD SCENARIO:
┌────────────┬────────────┬────────────┬────────────┐
│   State    │  Action    │  Unchecked │  Checked   │
├────────────┼────────────┼────────────┼────────────┤
│ Balance=   │  Deposit   │  Result=0  │  ERROR ✅  │
│ MAX        │  +1        │  ❌ WRAP   │  Safe      │
├────────────┼────────────┼────────────┼────────────┤
│ Balance=   │  Withdraw  │  Result=   │  ERROR ✅  │
│ 100        │  -101      │  MAX ❌    │  Safe      │
├────────────┼────────────┼────────────┼────────────┤
│ Price=     │  Multiply  │  Wraps to  │  ERROR ✅  │
│ 1B         │  * 1B      │  small ❌  │  Safe      │
└────────────┴────────────┴────────────┴────────────┘

SOLUTION:
┌──────────────────────────────────────────────────────────────┐
│  balance = balance.checked_add(amount)  ✅                   │
│                   .ok_or(ErrorCode::Overflow)?               │
│                                                               │
│  If would overflow: Returns None → Converted to Error        │
│  Transaction fails safely instead of corrupting state        │
└──────────────────────────────────────────────────────────────┘
```

---

### 4. Checks-Effects-Interactions Pattern

```
┌─────────────────────────────────────────────────────────────────┐
│            CHECKS-EFFECTS-INTERACTIONS PATTERN                  │
│             (Prevents Re-entrancy Attacks)                      │
└─────────────────────────────────────────────────────────────────┘

❌ VULNERABLE ORDER:
┌────────────────────────────────────────────────────────────┐
│ 1. ❌ INTERACTIONS (External call)                          │
│    invoke_signed(&transfer, &accounts, &seeds)?;           │
│    ↓ Attacker can re-enter here!                           │
│                                                             │
│ 2. ❌ EFFECTS (Update state)                               │
│    user.balance -= amount;  // Too late!                   │
│                                                             │
│ Problem: External call before state update                 │
│ Result: Attacker withdraws multiple times                  │
└────────────────────────────────────────────────────────────┘

✅ SECURE ORDER:
┌────────────────────────────────────────────────────────────┐
│ 1. ✅ CHECKS (Validate conditions)                         │
│    require!(user.balance >= amount);                       │
│    require!(vault.active);                                 │
│                                                             │
│ 2. ✅ EFFECTS (Update state FIRST)                        │
│    user.balance -= amount;  // Protected!                  │
│    vault.total_withdrawn += amount;                        │
│                                                             │
│ 3. ✅ INTERACTIONS (External calls LAST)                   │
│    invoke_signed(&transfer, &accounts, &seeds)?;           │
│                                                             │
│ Protection: State updated before external call             │
│ Result: Re-entrancy sees updated balance                   │
└────────────────────────────────────────────────────────────┘

ATTACK TIMELINE (Vulnerable):
┌─────┬─────┬─────┬─────┬─────┬─────┬─────┬─────┬─────┐
│ T0  │ T1  │ T2  │ T3  │ T4  │ T5  │ T6  │ T7  │ T8  │
├─────┼─────┼─────┼─────┼─────┼─────┼─────┼─────┼─────┤
│Start│Check│ CPI │Re-  │Check│ CPI │Re-  │ ... │Drain│
│Call │OK   │Call │Enter│OK!  │Call │Enter│     │Done │
│     │     │     │Back │     │     │Back │     │ 💰  │
└─────┴─────┴─────┴─────┴─────┴─────┴─────┴─────┴─────┘
              ↑     ↑           ↑     ↑
              │     └───────────┘     └─────────────┐
              │   Balance not yet    Balance still  │
              │   updated!           not updated!   │
              └──────────────────────────────────────┘
                   Exploit Window

DEFENSE TIMELINE (Secure):
┌─────┬─────┬─────┬─────┬─────┬─────┐
│ T0  │ T1  │ T2  │ T3  │ T4  │ T5  │
├─────┼─────┼─────┼─────┼─────┼─────┤
│Start│Check│Debit│ CPI │Re-  │FAIL │
│Call │OK   │Bal  │Call │Enter│ ✅  │
│     │     │✅   │     │Back │     │
└─────┴─────┴─────┴─────┴─────┴─────┘
                    ↑     ↑     ↑
                    │     │     └─── Check fails!
                    │     └───────── Balance = 0
                    └─────────────── CPI after debit
```

---

## Security Checklist Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│              PRE-DEPLOYMENT SECURITY CHECKLIST                  │
└─────────────────────────────────────────────────────────────────┘

LEVEL 1: CODE REVIEW
├─ [ ] All authorities use Signer<'info>
├─ [ ] All accounts use Account<'info, T>
├─ [ ] All PDAs have seeds + bump
├─ [ ] All math uses checked_*
├─ [ ] All CPIs use Program<'info, T>
├─ [ ] CEI pattern followed
└─ [ ] No manual deserialization

LEVEL 2: TESTING
├─ [ ] Happy path tests
├─ [ ] Authorization failure tests
├─ [ ] Exploit attempt tests
├─ [ ] Boundary value tests
├─ [ ] Integration tests
└─ [ ] Fuzz testing

LEVEL 3: STATIC ANALYSIS
├─ [ ] cargo clippy
├─ [ ] cargo audit
├─ [ ] soteria analyzer
└─ [ ] anchor verify

LEVEL 4: EXTERNAL REVIEW
├─ [ ] Peer code review
├─ [ ] Security audit (Neodyme/OtterSec/Sec3)
├─ [ ] Bug bounty program
└─ [ ] Community testing

LEVEL 5: DEPLOYMENT
├─ [ ] Verifiable build
├─ [ ] Multisig upgrade authority
├─ [ ] Monitoring setup
├─ [ ] Emergency procedures
└─ [ ] Rollback plan

┌─────────────────────────────────────────────────────────────────┐
│  ✅ ALL CHECKS PASSED → READY FOR MAINNET                      │
│  ❌ ANY CHECK FAILED → FIX BEFORE DEPLOYING                    │
└─────────────────────────────────────────────────────────────────┘
```

---

## Learning Path Flowchart

```
┌─────────────────────────────────────────────────────────────────┐
│                    LEARNING PATH                                │
└─────────────────────────────────────────────────────────────────┘

                    START HERE
                        │
                        ▼
            ┌─────────────────────┐
            │  Read Main README   │
            │  Understand basics  │
            └─────────┬───────────┘
                      │
                      ▼
            ┌─────────────────────┐
            │   BEGINNER PATH     │
            └─────────┬───────────┘
                      │
          ┌───────────┼───────────┐
          │           │           │
          ▼           ▼           ▼
    ┌─────────┐ ┌─────────┐ ┌─────────┐
    │ Missing │ │ Missing │ │ Account │
    │ Signer  │ │ Owner   │ │  Data   │
    │ Check   │ │ Check   │ │Matching │
    └────┬────┘ └────┬────┘ └────┬────┘
         │           │           │
         └───────────┼───────────┘
                     │
                     ▼
          ┌──────────────────────┐
          │   Read Security      │
          │   Guide (Part 1)     │
          └──────────┬───────────┘
                     │
                     ▼
          ┌──────────────────────┐
          │  INTERMEDIATE PATH   │
          └──────────┬───────────┘
                     │
          ┌──────────┼──────────┐
          │          │          │
          ▼          ▼          ▼
    ┌─────────┐ ┌─────────┐ ┌─────────┐
    │Arithm.  │ │Arbitrary│ │Re-entry │
    │Overflow │ │   CPI   │ │ Attack  │
    └────┬────┘ └────┬────┘ └────┬────┘
         │           │           │
         └───────────┼───────────┘
                     │
                     ▼
          ┌──────────────────────┐
          │   Study Anchor       │
          │   Constraints        │
          └──────────┬───────────┘
                     │
                     ▼
          ┌──────────────────────┐
          │    ADVANCED PATH     │
          └──────────┬───────────┘
                     │
                     ▼
          ┌──────────────────────┐
          │   Type Cosplay       │
          │   Complex Patterns   │
          │   Edge Cases         │
          └──────────┬───────────┘
                     │
                     ▼
          ┌──────────────────────┐
          │   Read Best          │
          │   Practices Guide    │
          └──────────┬───────────┘
                     │
                     ▼
          ┌──────────────────────┐
          │   Write Your Own     │
          │   Secure Program     │
          └──────────┬───────────┘
                     │
                     ▼
              🎓 EXPERT LEVEL
           Ready for Production!
```

---

## Anchor Constraint Cheat Sheet

```
┌─────────────────────────────────────────────────────────────────┐
│                 ANCHOR CONSTRAINTS QUICK REF                    │
└─────────────────────────────────────────────────────────────────┘

SIGNER VALIDATION:
├─ #[account(signer)]          → Requires signature
├─ pub authority: Signer       → Type-enforced signature
└─ authority.is_signer         → Manual check

OWNER VALIDATION:
├─ #[account(owner = program)] → Checks program ownership
├─ Account<'info, T>           → Auto-checks owner
└─ Program<'info, T>           → Enforces program type

PDA VALIDATION:
├─ seeds = [...]               → Verifies PDA derivation
├─ bump = account.bump         → Validates bump seed
└─ has_one = field             → Checks field matches

STATE VALIDATION:
├─ #[account(mut)]             → Marks mutable
├─ address = KNOWN_ADDR        → Verifies exact address
└─ constraint = condition      → Custom validation

INITIALIZATION:
├─ init                        → Creates new account
├─ payer = signer              → Who pays rent
├─ space = SIZE                → Account size
└─ init_if_needed              → Create if missing

CLEANUP:
└─ close = receiver            → Returns lamports

COMMON COMBOS:
┌──────────────────────────────────────────────────────────────┐
│ #[account(                                                    │
│   mut,                        // Can modify                  │
│   seeds = [b"vault", user],   // PDA check                   │
│   bump = vault.bump,          // Bump check                  │
│   has_one = authority,        // Relationship check          │
│   constraint = vault.active   // Custom check               │
│ )]                                                            │
│ pub vault: Account<'info, Vault>,                            │
│                                                               │
│ pub authority: Signer<'info>,  // Signature required        │
└──────────────────────────────────────────────────────────────┘
```

---

## Real-World Exploit Timeline

```
┌─────────────────────────────────────────────────────────────────┐
│          MAJOR SOLANA EXPLOITS TIMELINE                         │
└─────────────────────────────────────────────────────────────────┘

2021
────
Early 2021: Multiple small token programs
├─ Issue: Unchecked arithmetic
├─ Loss: ~$5M total
└─ Fix: Checked operations

2022
────
FEB 2022: Wormhole Bridge
├─ Issue: Missing signature verification
├─ Loss: $325M
└─ Fix: Proper signer checks

MAR 2022: Cashio Stablecoin
├─ Issue: Missing PDA verification
├─ Loss: $52M
└─ Fix: seeds + bump constraints

JUL 2022: Crema Finance
├─ Issue: Arbitrary CPI
├─ Loss: $8.8M
└─ Fix: Program type validation

2023
────
Multiple protocols: Arithmetic issues
├─ Issue: Unchecked math operations
├─ Loss: $45M+ total
└─ Fix: checked_* operations

┌─────────────────────────────────────────────────────────────────┐
│  TOTAL LOSSES FROM THESE PATTERNS: $435M+                      │
│                                                                  │
│  All preventable with proper validation! ✅                    │
└─────────────────────────────────────────────────────────────────┘
```

---

## Testing Strategy Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                    TESTING PYRAMID                              │
└─────────────────────────────────────────────────────────────────┘

                    ▲
                   ╱ ╲
                  ╱   ╲
                 ╱     ╲
                ╱       ╲
               ╱         ╲
              ╱           ╲
             ╱   Manual    ╲
            ╱    Security   ╲
           ╱     Audits      ╲
          ╱─────────────────── ╲
         ╱                      ╲
        ╱   Integration Tests    ╲
       ╱    (User Flows)          ╲
      ╱──────────────────────────── ╲
     ╱                               ╲
    ╱    Exploit Tests                ╲
   ╱     (Attack Scenarios)            ╲
  ╱──────────────────────────────────── ╲
 ╱                                       ╲
╱          Unit Tests                     ╲
           (All Instructions)              ╲
────────────────────────────────────────────

UNIT TESTS (Most)
├─ Test each instruction
├─ Test happy paths
├─ Test error conditions
└─ Fast execution

EXPLOIT TESTS (Many)
├─ Attempt unauthorized access
├─ Try fake accounts
├─ Test overflow/underflow
└─ Verify protections work

INTEGRATION TESTS (Some)
├─ Multi-instruction flows
├─ Cross-program interactions
├─ Complex user scenarios
└─ State consistency

SECURITY AUDITS (Few but Critical)
├─ Professional review
├─ Formal verification
├─ Economic analysis
└─ Final validation
```

---

_Visual guide to complement the comprehensive security patterns repository_
